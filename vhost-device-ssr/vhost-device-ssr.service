# SPDX-License-Identifier: BSD-3-Clause-Clear
# Copyright (c) Qualcomm Technologies, Inc. and/or its subsidiaries.

[Unit]
Description = launch vhost-device-ssr

After = ssr-rm.service tmp.mount early-services.target
Wants=early-services.target
Requires = ssr-rm.service
Before = qcrosvm.service shutdown.target
Conflicts = shutdown.target
DefaultDependencies = no

[Service]
Environment=RUST_LOG=info
ExecStart = /usr/bin/vhost-device-ssr --socket-path=/tmp/linux-vm2-ssr-skt --clients-groups=CDSP,CDSP1 --enable-sd-notify -p "vhost-device-ssr-la"
Type=notify
NotifyAccess=main
Slice=gvm.slice

[Install]
WantedBy = single-gvm.target
WantedBy = multi-gvm.target
WantedBy = safe-ivi.target
WantedBy = nonsafe-ivi.target
WantedBy = flex.target
